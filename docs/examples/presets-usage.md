# Presets Usage

*Added in v0.2.0*

Use built-in presets for common cluster configurations instead of specifying every parameter.

**What this example covers:**

1. List all built-in presets and their resource defaults
2. Look up a specific preset by name
3. Create a cluster from a preset with `dry_run=True`
4. Override preset defaults with explicit parameters

[:material-download: Download presets_usage.py](https://github.com/szaher/kuberay-sdk/blob/main/examples/presets_usage.py){ .md-button }

## Source Code

```python title="examples/presets_usage.py"
--8<-- "examples/presets_usage.py"
```
